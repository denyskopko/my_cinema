# 🎬 My Cinema — Movie Database Project

Учебный проект по созданию веб-интерфейса для базы данных фильмов **Sakila**.  
Проект объединяет работу с реляционными (**MySQL**) и нереляционными (**MongoDB**) базами данных в связке с современным фреймворком **FastAPI**.

---

## 🚀 Стек технологий
- **Backend:** Python 3.12+ / FastAPI  
- **Frontend:** Jinja2 Templates / HTML5 / CSS3 (Dark Theme)  
- **Database SQL:** MySQL (Sakila DB) + SQLAlchemy (ORM)  
- **Database NoSQL:** MongoDB (Хранение истории и аналитики поиска)  
- **Environment:** Python-dotenv для защиты данных  

---

## 🛠 Функционал
- **Главная страница:** Витрина с новинками фильмов и боковым меню жанров  
- **Поиск фильмов:**
  - По названию (частичное совпадение)
  - По диапазону годов выпуска (фильтрация в MySQL)
  - По жанрам (через таблицу связей `film_category`)
- **Пагинация:** Вывод строго по 10 результатов на страницу  
- **Аналитика (MongoDB):**
  - Сохранение каждого уникального поискового запроса
  - Учет частоты поиска каждого слова (счетчик `count`)
  - ТОП-5 популярных запросов («🔥 Популярное»)
  - 5 последних запросов («🕒 Недавние»)
- **Интерфейс:**
  - Полностью адаптивная темная тема
  - Динамическая подсветка активных жанров

---

## 📦 Инструкция по запуску

### 1. Подготовка окружения
Клонируйте репозиторий и создайте виртуальное окружение:

```bash
python -m venv venv
source venv/Scripts/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt


2. Настройка переменных окружения
Создайте файл .env в корневом каталоге:

DATABASE_URL=mysql+pymysql://user:password@localhost/db_name
MONGODB_URL_EDIT=mongodb+srv://...


3. Запуск приложения

Через Python:

python main.py

Или через Uvicorn:

uvicorn main:app --reload


Сайт будет доступен по адресу:
👉 http://127.0.0.1:8000


📂 Структура проекта:

📂 My_cinema
├── 📁 app
│   ├── __init__.py
│   ├── database.py          # Подключение MySQL (SQLAlchemy)
│   ├── models.py            # ORM-модели Sakila
│   ├── crud.py              # SQL-логика поиска и фильтрации
│   └── mongodb.py           # Аналитика и логирование запросов
├── 📁 static
│   └── style.css            # Modern Dark Theme
├── 📁 templates
│   ├── base.html            # Каркас сайта
│   ├── index.html           # Главная и поиск
│   └── genre.html           # Фильмы по жанру
├── .env                     # Переменные окружения (не коммитится)
├── main.py                  # Точка входа FastAPI
├── requirements.txt
└── README.md

